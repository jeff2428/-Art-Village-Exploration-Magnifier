[project]
name = "art-village-exploration-magnifier"
version = "0.1.0"
description = "Flet Web version of the Art Village Exploration Magnifier."
requires-python = ">=3.12"
dependencies = [
    "flet==0.85.1",
    "flet-camera",
]

[tool.flet]
permissions = ["camera"]
